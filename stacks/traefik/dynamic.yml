http:
  routers:
    headphones:
      rule: "Host(`headphones.kuipr.de`)"
      entryPoints:
        - websecure
      middlewares:
        # - authelia@docker
      service: headphones-service
      tls:
        certResolver: myresolver

  services:
    headphones-service:
      loadBalancer:
        servers:
          - url: "http://37.27.26.175:8181"
